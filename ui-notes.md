## 15 UI Details Observed from Findoc

1. Navbar height stays ~72px with centered alignment.
2. Container max-width is fixed around 1200px.
3. Hero heading uses large scale (~52px) with tight line-height.
4. Paragraph text is lighter gray (#555) for softness.
5. Buttons have rounded corners (~10–12px).
6. Button hover uses smooth transition (~200ms).
7. Navigation links reduce opacity on hover.
8. Hero section has large padding (~80px vertical).
9. Layout uses grid with 2-column split on desktop.
10. Mobile breakpoint triggers stacked column layout at 768px.
11. Feature cards have subtle border + shadow.
12. Cards maintain equal spacing (~24px gap).
13. CTA section background is slightly off-white (#fafafa).
14. Footer text uses smaller font size (~14px).
15. Overall spacing follows an 8px rhythm system.

